<!DOCTYPE html>
<html>
  <head>
    <title>Bank</title>
  </head>

  <body>
    <% if user_signed_in? %>
      <%= link_to "Sign Out", destroy_user_session_path %>
    <% end %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
